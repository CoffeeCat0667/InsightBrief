# 架构决策记录 (ARCHITECTURE DECISIONS)

> **日期**: 2026-08-20 Ver0.2.0 发布复核
> **状态**: 持续更新(Ver0.2.0 决策已落地)
> **范围**: 面向 Web 后端化改造的已确认决策, 只记录结论, 不涉及代码实现。

---

## 术语说明

- **PG** = **PostgreSQL**: 开源关系型数据库, 目标架构中的**持久化真相层**(文章、任务、简报等业务数据落库位置)。区别于 Redis(运行时状态层)。

---

## Ver0.2.0 已确认决策

| 编号 | 问题 | 决策 | 影响 |
|---|---|---|---|
| §0.2-1 | 是否继续仅人工触发抓取？ | **增加数据库持久化定时抓取** | `crawl_schedules` + FastAPI lifespan 后台线程；每 N 小时执行，服务重启恢复，当前不引入 Celery/APScheduler |
| §0.2-2 | 定时计划首次何时执行？ | **启用后立即执行** | `next_run_at=now`，首次创建真实 crawl task |
| §0.2-3 | 定时任务最多执行次数？ | **增加 max_runs，0 表示无限循环** | 达到正数上限自动暂停；冲突跳过不消耗次数 |
| §0.2-4 | 自动简报处理哪些文章？ | **仅本次抓取新增 inserted 文章** | `crawl_task_articles` 精确关联；existed/无新增不触发，避免历史文章重复送 LLM |
| §0.2-5 | 前端路由形式？ | **History API，彻底移除 Hash 路由** | URL 无 `#`；FastAPI/Nginx 需 SPA fallback |
| §0.2-6 | 管理能力范围？ | **增加 admin-only 管理面板** | 用户 CRUD、注册开关、LLM 在线探测双写、非管理员选项卡可见性 |
| §0.2-7 | 非管理员默认可见哪些选项卡？ | **文章、简报** | 抓取任务/新闻源可由 admin 配置；审计/管理面板始终隐藏 |
| §0.2-8 | LLM 配置修改何时落库？ | **probe 成功后同时写 JSON 与 PG** | probe 失败返回 upstream_error，配置不变；api_key 不写审计 |

---


| 编号 | 问题 | 决策 | 影响 |
|---|---|---|---|
| §15-1 | 是否单用户系统, 还是需要登录与用户体系? | **需要登录与用户体系** | 需实现 users/roles 表、注册/登录、鉴权(401/403 语义)、会话或 Token 机制; 涉及 audit_logs 审计 |
| §15-2 | PostgreSQL 是否必须独立部署, 还是允许 Docker Compose 一键启动? | **允许 Docker Compose 一键启动** | PG 纳入 Compose 服务编排, 提供一键部署形态 |
| §15-3 | Redis 是否允许直接部署, 还是同样纳入 Compose? | **同样纳入 Compose**(与 §15-2 一并"允许") | Redis 与 PG 统一在 docker-compose.yml 中管理 |
| §15-7 | 定时抓取是否由内部调度实现, 还是仅由用户手动触发? | **Ver0.1 阶段暂时人工; Ver0.2.0 起增加数据库持久化定时抓取** | 不引入 Celery/APScheduler; `schedule_manager` 由 FastAPI lifespan 启动, 人工触发仍保留 |
| §15-4 | 简报生成是否允许并发多个任务, 还是延续"同时只能一个"的约束? | **允许并发多个任务** | brief_worker 采用可并发消费模型, 不对同一时刻任务数做强限制 |
| §15-5 | 历史文章是否需要一次性导入到 PostgreSQL, 还是从新抓取开始? | **需要一次性导入** | 需提供一次性导入脚本/任务, 将现有 `data/*.json` 历史数据迁入 PG(按 `(source_id, external_id)` 去重) |
| §15-6 | 是否需要保留现有 `data/*.json` 作为只读历史档案? | **保留, 作为只读历史档案** | 代码不写入该目录, 仅作导入来源与查证; 写入路径全部切换为 PG |
| §15-8 | SSE 是唯一进度通道, 还是需要同时保留轮询接口作为降级方案? | **SSE 为唯一进度通道, 不保留轮询降级** | 进度只能通过 `GET /api/tasks/{id}/events` 获取; 任务状态快照仍存 Redis, 供新连接重放 |
| §15-9 | LLM api_key 是否进行密钥管理改造(脱敏/secret manager)? | **不做改造, 保持现状 (2026-08-18 拍板)** | LLM.json 明文 + 可选 `LLM_API_KEY` env 覆盖; 本地单机可接受, 生产部署时再评估密钥方案 |

## 已全部答复(9/9)

---

## 决策依据(对应目标架构原文要点)

- 登录体系 → 目标架构 users/roles/audit_logs 表、401/403 错误码、管理员与普通用户权限
- Compose 一键启动 → 目标架构 §15 待确认问题 #2/#3, 降低部署门槛
- 人工触发抓取 → 目标架构非目标中未要求调度器; 任务模型(pending→running→completed/failed/cancelled)与幂等 409 不受影响
- 简报并发 → 目标架构未限定单任务; 采用可并发 worker 消费模型
- 历史导入 + JSON 只读保留 → 导入脚本一次性迁移, 迁移完成后 `data/` 只读不再写入
- SSE 唯一通道 → 目标架构仅定义 SSE; 进度状态快照放 Redis 保证断线重连可重放
- History 路由 → 浏览器真实路径可分享/刷新；FastAPI 与未来 Nginx 均需对非 API 路径 fallback `index.html`
- 定时抓取 + 自动简报 → 数据库为配置真相，调度器只创建真实 crawl task；文章关联表避免重复简报